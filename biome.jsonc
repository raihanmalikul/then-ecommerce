{
  "$schema": "./node_modules/@biomejs/biome/configuration_schema.json",
  "extends": [
    "ultracite/biome/core",
    "ultracite/biome/react",
    "ultracite/biome/tanstack",
    "ultracite/biome/vitest"
  ],
  "files": {
    "includes": [
      // Vendored agent skills. They carry their own scripts, none of it ships
      // to the Worker, and reformatting somebody else's tool to this store's
      // rules would be wrong.
      "!.agents",
      "!dist",
      "!drizzle/meta",
      "!.output",
      "!src/components/ui",
      "!src/logo.svg",
      "!src/routeTree.gen.ts",
      "!src/styles.css"
    ]
  },
  "linter": {
    "rules": {
      "correctness": {
        "useImageSize": "off"
      },
      "nursery": {
        "useSortedClasses": "off"
      },
      "performance": {
        "noJsxPropsBind": "off"
      },
      "style": {
        "useConsistentTypeDefinitions": "off"
      }
    }
  }
}
