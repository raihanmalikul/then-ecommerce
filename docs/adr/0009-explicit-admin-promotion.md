# Require explicit admin promotion

The setup command will not promote an existing account silently. It will fail when the requested admin email already exists and will require a separate explicit promotion action.
